apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: {{NAMESPACE}}
spec:
  type: NodePort
  selector:
    app: myapp
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: {{PORT}}
